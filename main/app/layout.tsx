import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TokenSniper - AI-Powered Web3 Token Analysis & Auto-Investment",
  description:
    "Connect your Telegram, let our AI analyze group sentiment, and automatically invest in promising web3 tokens before they moon. Snipe the next 100x token before anyone else.",
  keywords:
    "web3, tokens, telegram, AI, sentiment analysis, auto investment, crypto trading, token sniper",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
